---
schema: 1
legacy_commit: 400f03f276ae1bb69f5460c175b9fb923d620f1a
stacks:
  audiobookshelf:
    TZ: {classification: inventory, target: nas_timezone}
  beszel:
    BESZEL_AGENT_KEY: {classification: vault, target: vault_beszel_agent_key}
    BESZEL_AGENT_TOKEN: {classification: vault, target: vault_beszel_universal_token}
    BESZEL_APP_URL: {classification: role, target: beszel_app_url}
    BESZEL_SYSTEM_NAME: {classification: role, target: beszel_system_name}
    TZ: {classification: inventory, target: nas_timezone}
  dozzle:
    TZ: {classification: inventory, target: nas_timezone}
  immich:
    DB_DATABASE_NAME: {classification: vault, target: vault_immich_db_name}
    DB_PASSWORD: {classification: vault, target: vault_immich_db_password}
    DB_USERNAME: {classification: vault, target: vault_immich_db_username}
    TZ: {classification: inventory, target: nas_timezone}
  jellyfin:
    TZ: {classification: inventory, target: nas_timezone}
  komga:
    GROUP_ID: {classification: inventory, target: nas_gid}
    TZ: {classification: inventory, target: nas_timezone}
    USER_ID: {classification: inventory, target: nas_uid}
  ntfy:
    GROUP_ID: {classification: inventory, target: nas_gid}
    NTFY_BASE_URL: {classification: role, target: ntfy_base_url}
    TZ: {classification: inventory, target: nas_timezone}
    USER_ID: {classification: inventory, target: nas_uid}
  paperless-ngx:
    DB_NAME: {classification: vault, target: vault_paperless_db_name}
    DB_PASSWORD: {classification: vault, target: vault_paperless_db_password}
    DB_USER: {classification: vault, target: vault_paperless_db_username}
    GROUP_ID: {classification: inventory, target: nas_gid}
    PAPERLESS_AI_ENABLED: {classification: role, target: paperless_ai_enabled}
    PAPERLESS_AI_LLM_ENDPOINT: {classification: role, target: paperless_ai_llm_endpoint}
    PAPERLESS_AI_LLM_MODEL: {classification: role, target: paperless_ai_llm_model}
    PAPERLESS_SECRET_KEY: {classification: vault, target: vault_paperless_django_secret_key}
    PAPERLESS_TASK_WORKERS: {classification: role, target: paperless_task_workers}
    PAPERLESS_THREADS_PER_WORKER: {classification: role, target: paperless_threads_per_worker}
    TZ: {classification: inventory, target: nas_timezone}
    USER_ID: {classification: inventory, target: nas_uid}
  tinymediamanager:
    GROUP_ID: {classification: inventory, target: nas_gid}
    PASSWORD: {classification: vault, target: vault_tinymediamanager_password}
    TZ: {classification: inventory, target: nas_timezone}
    USER_ID: {classification: inventory, target: nas_uid}
