---
# Pinned like every other dependency. community.docker provides
# docker_compose_v2, which reports real change status and supports check mode.
# Compose needs the Docker CLI plugin; the pinned network and container-info API
# modules additionally require requests in the managed target interpreter.
collections:
  - name: community.docker
    version: 5.2.2
