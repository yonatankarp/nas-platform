---
services:
  ntfy:
    container_name: !reset null
    ports: !override
      - "127.0.0.1:${NTFY_HOST_PORT:?}:80"
    volumes: !override
      - ${PLATFORM_MAC_SANDBOX:?}/legacy/ntfy/cache:/var/cache/ntfy
      - ${PLATFORM_MAC_SANDBOX:?}/legacy/ntfy/data:/var/lib/ntfy
