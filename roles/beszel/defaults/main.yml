---
beszel_port: 8090
beszel_app_url: "http://{{ platform_public_host }}:{{ beszel_port }}"
beszel_system_name: ASUSTOR-AS6704T

# Beszel persists one sample per minute. Three samples of freshness tolerance
# avoids false negatives from scheduling jitter while still rejecting a healthy
# agent that has stopped writing collection records. A 90-second poll spans the
# next expected sample without making verification unbounded.
# Host inventories must select an exact supported policy. Empty defaults fail
# closed if a new host class does not declare its telemetry capability.
beszel_required_telemetry_categories: []
beszel_require_gpu_telemetry: false
beszel_telemetry_freshness_seconds: 180
beszel_telemetry_poll_timeout_seconds: 90
beszel_telemetry_request_timeout_seconds: 3
beszel_telemetry_poll_delay_seconds: 3

# Alerts to maintain for the monitored system. name must be one of Beszel's alert
# types; value and min are its threshold and sustain period.
beszel_alerts:
  - name: Status
    value: 0
    min: 0
  - name: CPU
    value: 90
    min: 10
  - name: Memory
    value: 90
    min: 10
  - name: Disk
    value: 85
    min: 10
  # Watches CPU, NVMe and cooling, not the SATA bay disks: the hub's temperature
  # panel on 2026-09-13 showed coretemp package/cores at ~64°C with spikes to
  # ~85°C under load, NVMe at ~51-54°C, acpitz at ~28°C, and no sensor for the
  # bay disks at all (SMART, #607, is what would cover those). In 0.19.0 an alert
  # with min above 1 fires only when the hottest sensor right now is above value
  # AND the hottest sensor's own mean over min minutes is above it, strictly
  # (internal/alerts/alerts_system.go:89-131,325-356). No PRIMARY_SENSOR is set,
  # so "hottest" is the max across sensors. A load spike to 85 is therefore not
  # enough; fifteen minutes averaging above 85 is a cooling failure.
  - name: Temperature
    value: 85
    min: 15

# Shoutrrr URL. Built from the Pushover application token and user key in vault,
# so the value Beszel stores is authored in one place rather than copied between
# two web interfaces -- the same property the ntfy form had, over a transport
# that reaches a phone without the NAS being reachable from it. Pushover itself
# takes only the application token and the user key, which are this URL's
# password and host and are what the two vault values are. The `shoutrrr` user
# component is a filler the library reads and discards: `pushover://:token@userkey/`
# is accepted just the same, measured against this pin. It is kept because an
# empty user component reads as a typo, not because anything requires it.
#
# This is the Alerts application (vault_pushover_alerts_token): host and platform
# problems, silent while healthy. Priority 1, which bypasses the recipient's quiet
# hours without an acknowledgement loop, because a host threshold breach
# overnight is worth waking a phone for.
#
# KNOWN AND ACCEPTED: Beszel sends its recovery notice ("... below threshold")
# through this same URL, so a recovery also rings at priority 1. Beszel builds
# both from one stored webhook and has no per-event priority, and routing Beszel
# through the relay to separate them is out of scope for #558.
#
# How the query reaches Pushover, read from source at the pins rather than
# assumed. Beszel 0.19.0's go.mod requires github.com/nicholas-fedor/shoutrrr
# v0.19.0, NOT containrrr/shoutrrr. Beszel's SendShoutrrrAlert
# (internal/alerts/alerts.go) parses this URL, takes its existing query, Adds
# `title` to it and re-encodes it, so `priority` survives beside the title. The
# fork's pkg/services/push/pushover/pushover_config.go declares
# `Priority int8 key:"priority"` and setURL feeds every query key through the
# config resolver; pushover.go's sendToDevice then sets the form's `priority`
# only when it lies in -2..1. So 1 reaches the API. 2 would be parsed and then
# silently NOT sent, which is what an earlier revision of this comment got wrong
# when it said 2 was "carried through to the API call"; 200 is refused by int8
# parsing. That gate, and the fork carrying no retry or expire key -- asked for
# one it answers `not a valid config key: expire (valid keys: [devices
# encryptionkey key priority title])` -- are why the emergency priority is only
# reachable through the Dozzle relay, which POSTs the API itself.
beszel_notification_url: >-
  pushover://shoutrrr:{{ vault_pushover_alerts_token }}@{{ vault_pushover_user_key }}/?priority=1

# Pushover's own credential check, which is the only thing on this platform that
# can tell a working notification chain from a stored string. Pushover documents
# validation requests as exempt from the message quota, so running one on every
# verification costs nothing and sends nothing to anybody's phone.
#
# A variable rather than a literal for the reason
# production_auto_deploy_github_api_base is one: the endpoint has to be
# redirectable to prove the task's own branches without involving the real
# service.
beszel_pushover_validation_url: https://api.pushover.net/1/users/validate.json
