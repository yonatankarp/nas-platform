---
# The single operator switch, mirrored from inventory/group_vars/all/main.yml so
# the role reads correctly on its own. False here and there: PR 1 lands the whole
# stack without converging it anywhere. The gate lives inside the role rather
# than on the site.yml entry, because a `when:` there would also skip the
# deployment_bundle re-include that revalidates the paths this role touches and
# the deployment report that says whether anything moved. Flip
# seafile_deployment_enabled to true in inventory and merge, and the auto-deploy
# poller brings the stack up on its next tick.
seafile_deployment_enabled: false

# 8083. The next free host port on this platform: 8080 is Dozzle, 8081 the
# Dozzle alert relay, 8082 is already claimed by the qbittorrent web interface
# declared in config/media-acquisition.yml, 8085 is SABnzbd and 8090 is Beszel.
seafile_port: 8083

# Every role addresses services over 127.0.0.1, which is correct whether the
# play runs on the NAS itself or over SSH from a workstation. /api2 is the
# stable REST prefix seahub mounts; the bundled nginx multiplexes seahub,
# seafhttp and seafdav behind the single published port.
seafile_api: "http://127.0.0.1:{{ seafile_port }}/api2"

seafile_data_host_path: "{{ nas_docker_root }}/seafile/data"
seafile_db_host_path: "{{ nas_docker_root }}/seafile/db"

# Seafile comes up in two stages, so the two waits are sized separately. MariaDB
# alone reaches its healthcheck quickly, but --mariadbupgrade keeps that check
# red for as long as an in-place upgrade runs. The server then creates three
# databases, runs every pending migration and starts seaf-server and seahub.
# nginx is already listening on 80 before that sequence starts -- enterpoint.sh
# blocks until it is -- and answers 502 until seahub binds, which is why the
# server's health check is a seahub probe rather than a port check;
# services/seafile/compose.yml records the measurement.
#
# NEITHER NUMBER IS THE BOOT BUDGET. The health checks in
# services/seafile/compose.yml are, and this is worth stating flatly because the
# first CI run of the seafile lane was read the other way round. Compose's
# `--wait` stops the moment Docker reports a container *unhealthy*, and Docker
# reports that after `start_period` plus `retries` consecutive failing probes
# `interval` apart:
#
#   server (start_period 300s, retries 5, interval 30s) -> at most 450s
#   db, cache (start_period  60s, retries 5, interval 30s) -> at most 210s
#
# Measured on the failing CI run of 2026-09-07: the server was declared
# unhealthy at ~424s and the play failed there, so the 600 below never bound.
# 424 rather than 450 because the probe that lands on the start_period boundary
# is already outside it and counts as the first of the five, putting the fifth
# at 300 + 4x30 = 420s plus one probe's own runtime. The bound sized against
# above is the looser 300 + 5x30, because it does not depend on how Docker
# happens to schedule that boundary probe.
# The knob was not dead by accident -- these two values are the backstop for the
# other failure, the one where Compose returns no health verdict at all -- but a
# reader who takes 600 for "how long Seafile may take to boot" is reading the
# wrong file. The invariant is the ordering: each wait must stay strictly ABOVE
# its stage's worst-case verdict time, so that a wedged boot surfaces as Docker's
# own `container <name> is unhealthy`, which names the container, rather than as
# a bare module timeout, which names nothing. 600 > 450 and 300 > 210 hold, and
# that is the whole of what these numbers are for.
#
# Deliberately NOT raised, and it is now known that raising anything would not
# have helped. The passing run before that one (13a00da) reported the server
# healthy 13.6s after its container was created, from an empty /shared and an
# empty MariaDB datadir. So the health check already grants roughly thirty
# times what the only first boot anyone has measured needed -- 420s of budget
# against 13.6s of boot -- and a server still silent at the end of that was not
# one a larger budget would have caught. The mechanism is in the image, and
# services/seafile/compose.yml carries it: enterpoint.sh idles for ever whether
# start.py exits or merely stalls in one of its unbounded waits, so a setup that
# does not finish leaves a container that runs and never serves. The seafile
# lane dumps the health log and the Compose logs on a converge failure
# (dump_seafile_diagnostics in tests/integration_controller_lib.sh); that
# evidence is what would name the next occurrence, and there is no number here
# that could pre-empt it.
seafile_data_compose_wait_timeout: 300
seafile_compose_wait_timeout: 600

# How many times a wedged server container may be force-recreated before the
# converge fails. The mechanism, the measurements and why nothing self-heals are
# in roles/seafile/tasks/deploy.yml beside the rescue that spends this budget.
#
# 1, and the number is small on purpose. This is a recovery from one observed
# upstream defect -- enterpoint.sh launches start.py once and idles for ever, so
# a start.py that raised or stalled leaves a container that runs and never serves
# -- and a wedge that survives a recreate from an untouched image against an
# untouched volume is a broken deployment rather than an unlucky one. Raising
# this buys more identical 424-second waits, not more information.
#
# 0 is a supported value and is the one to set while debugging a live wedge: the
# converge still detects the state and still prints the container's health log,
# and then fails without touching the evidence.
seafile_wedged_boot_recreate_limit: 1

# The role's own readiness budget, declared here rather than read from
# platform_readiness_retries, because the shared value is explicitly sized for
# "a container that has already reported healthy" and the one poll that reads it
# is the one after a restart, where it has not. The target is the 300-second
# start_period on this server's own health check in services/seafile/compose.yml
# -- the estimate already made there of how long seahub takes to answer, reused
# rather than guessed a second time -- and 100 attempts spaced at the shared
# platform_readiness_delay covers it. Only the patience deviates: the delay stays
# shared, so this number is the one thing to change if that target moves.
seafile_readiness_retries: 100

seafile_compose_project_name: >-
  {{ (platform_project_name ~ '-seafile')
     if platform_project_name | default('') | length > 0 else 'seafile' }}

# What [INDEX FILES] enabled must say in seafevents.conf. Seafile Pro writes
# `true` there on first run and bootstrap.py writes `es_host = elasticsearch`
# beside it, but this platform does not deploy Elasticsearch yet -- it needs the
# vm.max_map_count host sysctl nothing here manages and the repository's first
# mem_limit. Left alone the index updater resolves that hostname every ten
# minutes forever and logs a failure each time, so the setting is switched off
# explicitly and reconciled by tasks/reconcile_seafevents.yml.
seafile_index_files_enabled: false

# What [AUDIT] enabled must say in the same file. True: Seafile Pro keeps a real
# audit log -- FileAudit, FileUpdate and PermAudit in seahub_db -- and #445 makes
# it a requirement rather than a preference, because this is the first service
# whose contents are the user's own documents and "who read this file" is a
# question the platform must be able to answer afterwards.
#
# It is declared here rather than left alone even though the image already
# writes `true`, and the difference matters. Read out of pro.py's
# generate_seafevents_conf in seafileltd/seafile-pro-mc:13.0.27, the first-run
# template carries `[AUDIT] enabled = true` -- so today the audit log is on by
# coincidence of an upstream default, not by a decision this repository records.
# seafevents' own reader is where that becomes load-bearing: app/config.py's
# is_audit_enabled and app/mq_handler.py's init_message_handlers both fall
# through to `enable_audit = False` when the key is absent, so an image that
# stopped writing it would switch auditing off silently and nothing here would
# notice. Owning the key turns that into a repair the converge performs and
# reports.
seafile_audit_log_enabled: true

# The settings in seafevents.conf this platform owns, as (section, key, value)
# rather than as a key and a value. The section is the whole point.
#
# `enabled` is not a unique key in this INI document. The first-run template in
# pro.py declares one under EVERY one of its five sections -- [SEAHUB EMAIL],
# [STATISTICS] (written `enabled=true`, with no spaces around the `=`), [AUDIT],
# [INDEX FILES] and [FILE HISTORY] -- so a transform that rewrites `enabled` per
# line switches off four features to change one, and reports itself converged
# having done it. That is not hypothetical: it is what the line-scoped first
# draft of this reconciliation did to the audit log, which is why this list
# names a section for every value and why tasks/reconcile_seafevents.yml builds
# its patterns from that section rather than from the key alone.
#
# Adding a row here is how this platform takes over another seafevents setting.
# Nothing else has to change: the repair, the report and the restart all loop
# over this list.
seafile_seafevents_managed_settings:
  - section: INDEX FILES
    key: enabled
    value: "{{ seafile_index_files_enabled | bool | lower }}"
  - section: AUDIT
    key: enabled
    value: "{{ seafile_audit_log_enabled | bool | lower }}"

# conf/ inside the bind-mounted volume, which is where the server keeps the
# files its setup generated. The volume is mounted at /shared and the server
# lays out /shared/seafile/conf/, so seafevents.conf lands here.
#
# That layout is read out of seafileltd/seafile-pro-mc:13.0.27 itself, not
# inferred from the manual. The server generates its configuration at
# /opt/seafile/conf; bootstrap.py's files_to_copy moves `conf` to
# /shared/seafile/conf on the first setup and symlinks it back, and
# /scripts/create_data_links.sh re-establishes that symlink on every later start.
# /shared is this bind mount, so the file lands here.
#
# The reconciliation is still written to survive being wrong about it, because
# PR 1 ships the stack switched off and no container has created the file yet: a
# path that does not exist is the "nothing to reconcile" branch, and a file that
# exists without an [INDEX FILES] enabled key is reported by name rather than
# silently treated as converged. Both of those reports are what to read on the
# first converge after the flag is flipped.
seafile_seafevents_config_path: "{{ seafile_data_host_path }}/seafile/conf/seafevents.conf"

# seafile.conf, beside seafevents.conf in the same generated conf/ directory and
# written by the same first-run setup: setup-seafile-mysql.py generates it and
# bootstrap.py appends to it, both inside init_seafile_server(), which returns
# early once /shared/seafile/seafile-data exists. Grepped across /scripts and the
# server directory of seafileltd/seafile-pro-mc:13.0.27, nothing else writes it
# on an ordinary start -- only the cluster setup script and the major-version
# upgrade scripts, and both of those append. So the same repair-then-restart
# shape that converges for seafevents.conf converges here.
#
# Secret-bearing, like the file beside it: seafile.conf carries the Seafile
# database account and its password under [database]. Every task that reads or
# writes it is redacted for that reason.
seafile_server_config_path: "{{ seafile_data_host_path }}/seafile/conf/seafile.conf"

# The per-user storage quota, and the reason there is one at all: Seafile is the
# only service here whose stored volume is chosen by a person rather than by a
# library that has a size. #445 states the failure directly -- left unset, one
# user fills the volume and takes every other stack down with it -- and the
# volume in question is /volume1, the NVMe service-state volume that carries
# every other stack's database, not the media volume.
#
# ## What reads it, established by disassembling seaf-server rather than read
#    out of the manual
#
# seaf-server resolves a user's quota in three steps, and the third is this
# value. Read out of quota-mgr.c in
# seafileltd/seafile-pro-mc:13.0.27 at instruction level:
#
#   1. `SELECT quota FROM UserQuota WHERE "user"=?` -- a quota an administrator
#      set on one account. Returned if positive or -2 (unlimited).
#   2. `SELECT quota FROM RoleQuota WHERE role=?` -- the role quota seahub
#      writes. Returned if positive or -2.
#   3. `[quota] default` from seafile.conf, which is this key.
#
# This platform sets neither of the first two, so this is the value every
# account resolves to, and it is the only one of the three that a converge can
# push. The role quota is reachable only from seahub_settings.py's
# ENABLED_ROLE_PERMISSIONS, and every call to set_role_quota in the image sits
# on a LOGIN path -- the web login form, the remote-user and Shibboleth
# backends, the OAuth and ADFS handlers -- none of which is the path
# POST /api2/auth-token/ takes. A quota declared there would not exist until
# somebody signed in to the web interface, which is a worse guarantee than one
# the server reads at start.
#
# ## Why the value looks like `20g`
#
# seaf-server's own parser takes a decimal number followed by an optional
# k/kb/m/mb/g/gb/t/tb suffix, and a bare number means GIGABYTES -- the empty
# suffix multiplies by 10^9. A value it cannot parse is not an error either: it
# logs "Invalid default quota" and falls back to unlimited, which is a silent
# no-quota. The suffix is written out here so the unit is on the page rather
# than in a reader's memory, and tasks/reconcile_quota.yml refuses a spelling
# the parser would reject before the run reaches the target.
#
# ## Why 20 GB, when nothing here has measured /volume1
#
# It is deliberately small, and the argument for it is an asymmetry rather than
# a measurement. Raising a quota is free and takes effect at the next server
# start; lowering one below what a user already stores leaves that user over
# quota, unable to upload, with data the platform cannot delete for them. So the
# value to ship is the smallest one that is plausibly useful, and the operator
# raises it against a number this repository does not have. Before raising it,
# run `df -h /volume1` on the NAS and record what is actually free: the Pro
# edition is free to three users, so the worst case this bounds is three times
# whatever is set here, and at 20g that is 60 GB of a volume sixteen stacks
# share.
seafile_default_user_quota: 20g

# Where the pre-upgrade backup lands. A third root beside the data and the
# database, and separate from both on purpose: a backup inside the tree it
# protects is not a backup, and a dump inside /var/lib/mysql would be a
# directory MariaDB scans as if it were a schema.
#
# 0700 in nas_storage, unlike the two 0755 roots beside it, because what lands
# here is secret-bearing in a way the live trees are not. mariadb-dump of
# ccnet_db and seahub_db carries every account row Seafile holds -- password
# hashes, session material and API tokens included -- as plain readable SQL,
# and the conf/ copy carries the Seafile database account and the JWT signing
# key. CLAUDE.md's security boundary names this directory for that reason.
seafile_backup_host_path: "{{ nas_docker_root }}/seafile/backups"

# The three schemas Seafile's own setup creates, dumped together in one
# transaction so they are consistent with each other. Order matters to nothing
# here -- --databases writes them into one file -- but the list is stated once
# and read by the dump, the manifest and the contract rather than spelled three
# times.
seafile_backup_databases:
  - ccnet_db
  - seafile_db
  - seahub_db

# What a full backup must leave out, rendered into every backup's manifest so
# the operator holding one reads it beside the dump rather than in a document
# somewhere else.
#
# `pro-data/search` is a Seafile-side index. It is regenerable from the files it
# indexes and is `recovery: cache` in kind even though it sits inside a
# `recovery: critical` root, so carrying it doubles a backup that restores no
# faster for it. `logs` is diagnostics. `conf/admin.txt` is the one entry here
# that is a security requirement rather than a size one: /scripts/start.py
# writes the administrator password there in plaintext on every container start
# and removes it in a `finally:`, so a container killed mid-start leaves it on
# disk, and a backup that swept conf/ blindly would preserve that plaintext for
# as long as the backup is kept.
seafile_backup_excluded_paths:
  - pro-data/search
  - logs
  - conf/admin.txt

# How many pre-upgrade backups are kept. Three, so the two upgrades before the
# current one are still recoverable, and bounded at all because this directory
# sits on the same volume as the database it protects: an unbounded backup root
# on /volume1 is a way to lose the live copy as well.
#
# Pruning happens only on a run that took a backup, so a converge that changes
# nothing removes nothing.
seafile_pre_upgrade_backup_retain: 3

# Take the backup even when no upgrade is pending. False everywhere but the
# seafile integration lane, which sets it so the restore rehearsal exercises
# THIS role's backup rather than a copy of the same commands written inside the
# contract -- a rehearsal that restores its own dump proves the rehearsal, not
# the platform.
#
# It cannot manufacture a backup out of nothing: with the stack not running
# there is no database to dump, and this flag does not change that branch.
seafile_pre_upgrade_backup_force: false
