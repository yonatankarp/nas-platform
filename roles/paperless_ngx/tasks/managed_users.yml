---
# Every exec in this file states failed_when, and none of them is decoration.
# Measured against community.docker 5.3.0: docker_compose_v2_exec sets check_rc
# only when `detach` is true, so every other invocation returns whatever exit
# code the command produced and Ansible reports success regardless. The scripts
# in files/ are written to raise rather than to return a status -- an assert
# that a token belongs to the user it was proven against, a group set whose
# length must match, a transaction that rolls back -- so rc carries all of it,
# and a task without this line converts a deliberate refusal into a clean recap.
#
# Every one of them is also no_log, so no message accompanies the refusal: the
# writes pass the managed password or the managed token through `env:`, the two
# listings put every managed username and email on stdout, and Ansible censors
# the whole result rather than a chosen field. Which stream would have to be
# read if one ever could be: Django writes the traceback to stderr, the
# listing scripts print their JSON to stdout, and a message naming one of the
# two is the wager tests/contracts/nextcloud-runtime.rb's diagnosis helper
# exists to avoid. So the task name is the whole of what a reader gets, which is
# a reason to keep these names as specific as they are: no_log censors an
# assert's fail_msg exactly as it censors a module result, and the banner
# Ansible prints above the censored line is the one field it does not touch.
#
# include_tasks never applies meta/argument_specs.yml, and the tasks below are
# gated on this phase, so an unrecognised value would silently skip them while
# the run still reported success. verify.yml reaches its verification tasks
# through exactly this mechanism.
- name: Validate the Paperless managed-user phase
  ansible.builtin.assert:
    that:
      - paperless_managed_users_phase in ['reconcile', 'verify']
    fail_msg: >-
      Paperless managed-user reconciliation was included with a phase it does not
      implement. The tasks in this file are gated on paperless_managed_users_phase,
      so an unrecognised phase silently skips them.

# Redacted: the listing carries vault_paperless_admin_username and every
# managed username and email, even though it omits password hashes.
- name: List complete sanitized Paperless users for managed-user reconciliation
  community.docker.docker_compose_v2_exec:
    project_src: "{{ platform_current_dir }}/services/paperless-ngx"
    project_name: "{{ paperless_compose_project_name }}"
    files: "{{ platform_service_compose_files['paperless-ngx'] }}"
    env_files: ["{{ platform_runtime_dir }}/services/paperless-ngx/.env"]
    service: webserver
    argv:
      - python
      - manage.py
      - shell
      - -c
      - "{{ lookup('ansible.builtin.file', role_path ~ '/files/list_users.py') }}"
    tty: false
  register: paperless_managed_users_probe
  changed_when: false
  check_mode: false
  # The listing every decision in this file is taken from. Without it a failed
  # read fell through to the set_fact below, which parses the last stdout line
  # as JSON -- and a listing that exited nonzero while still printing a parsable
  # line would have been reconciled against as though it were the user set.
  failed_when: paperless_managed_users_probe.rc | default(1) | int != 0
  no_log: true

- name: Resolve sanitized Paperless managed-user listing
  ansible.builtin.set_fact:
    paperless_managed_users_listing: "{{ paperless_managed_users_probe.stdout_lines | last | from_json }}"
  no_log: true

- name: Resolve normalized Paperless managed identities
  ansible.builtin.set_fact:
    paperless_managed_user_matches: >-
      {{ (paperless_managed_user_matches | default({})) |
         combine(dict([(item.username, paperless_managed_users_listing |
           selectattr('username', 'equalto', item.username) | list)])) }}
  loop: "{{ vault_managed_paperless_ngx_users }}"
  loop_control:
    label: paperless-managed-user
  no_log: true

- name: Initialize Paperless managed-user binding facts
  ansible.builtin.set_fact:
    paperless_initial_authenticated_managed_user_ids: >-
      {{ paperless_initial_authenticated_managed_user_ids | default({}) }}
  no_log: true

# The two conditions this replaces are the one decision all six HTTP-driven
# managed-user roles share; filter_plugins/managed_user_identity.py explains why
# they are also the only one, and carries them for every service. Paperless is
# the role that proves the listing is not a parameter: this one arrives as JSON
# on the stdout of a management command inside the container, not from an API.
- name: Refuse ambiguous normalized Paperless managed identities
  ansible.builtin.assert:
    that:
      - >-
        paperless_managed_users_listing | managed_user_ambiguity_errors(
          'username', item.username,
          paperless_managed_user_matches[item.username]
        ) | length == 0
    fail_msg: >-
      Paperless contains duplicate normalized managed usernames:
      {{ paperless_managed_users_listing | managed_user_ambiguity_errors(
           'username', item.username,
           paperless_managed_user_matches[item.username]
         ) | join('; ') }}
  loop: "{{ vault_managed_paperless_ngx_users }}"
  loop_control:
    label: paperless-managed-user
  no_log: true

- name: Authenticate existing Paperless managed users
  ansible.builtin.uri:
    url: "{{ paperless_api }}/api/token/"
    method: POST
    body_format: json
    body:
      username: "{{ item.username }}"
      password: "{{ item.password }}"
    status_code: "{{ range(100, 600) | list }}"
    return_content: true
  register: paperless_managed_user_authentication
  when:
    - not ansible_check_mode
    - paperless_managed_user_matches[item.username] | length == 1
  loop: "{{ vault_managed_paperless_ngx_users }}"
  loop_control:
    label: paperless-managed-user
  failed_when: false
  changed_when: false
  no_log: true

- name: Require preserved Paperless managed-user credentials
  ansible.builtin.assert:
    that:
      - item.status | default(0) | int == 200
      - item.json.token | default('') | length > 0
    fail_msg: >-
      Existing Paperless managed user does not accept its preserved vault password.
      Run the reviewed credential-migration procedure; Ansible will not reset it automatically.
  loop: "{{ paperless_managed_user_authentication.results | default([]) | rejectattr('skipped', 'defined') | list }}"
  loop_control:
    label: paperless-managed-user
  when:
    - not ansible_check_mode
  no_log: true

- name: Resolve authenticated existing Paperless managed-user bindings
  ansible.builtin.set_fact:
    paperless_initial_authenticated_managed_user_ids: >-
      {{ (paperless_initial_authenticated_managed_user_ids | default({})) |
         combine({item.item.username:
           (paperless_initial_authenticated_managed_user_ids | default({})).get(
             item.item.username,
             paperless_managed_user_matches[item.item.username][0].id
           )}) }}
    paperless_authenticated_managed_user_ids: >-
      {{ (paperless_authenticated_managed_user_ids | default({})) |
         combine({item.item.username:
           paperless_managed_user_matches[item.item.username][0].id}) }}
    paperless_authenticated_managed_user_tokens: >-
      {{ (paperless_authenticated_managed_user_tokens | default({})) |
         combine({item.item.username: item.json.token}) }}
  loop: "{{ paperless_managed_user_authentication.results | default([]) | rejectattr('skipped', 'defined') | list }}"
  loop_control:
    label: paperless-managed-user
  when: not ansible_check_mode
  no_log: true

- name: Report planned Paperless managed-user creation
  ansible.builtin.debug:
    msg: PAPERLESS_PLAN_MANAGED_USER_CREATE
  when:
    - paperless_managed_users_phase == 'reconcile'
    - ansible_check_mode
    - paperless_managed_user_matches[item.username] | length == 0
  loop: "{{ vault_managed_paperless_ngx_users }}"
  loop_control:
    label: paperless-managed-user
  changed_when: true

- name: Create absent Paperless managed users
  community.docker.docker_compose_v2_exec:
    project_src: "{{ platform_current_dir }}/services/paperless-ngx"
    project_name: "{{ paperless_compose_project_name }}"
    files: "{{ platform_service_compose_files['paperless-ngx'] }}"
    env_files: ["{{ platform_runtime_dir }}/services/paperless-ngx/.env"]
    service: webserver
    env:
      MANAGED_USERNAME: "{{ item.username }}"
      MANAGED_PASSWORD: "{{ item.password }}"
      MANAGED_EMAIL: "{{ item.email }}"
    argv:
      - python
      - manage.py
      - shell
      - -c
      - "{{ lookup('ansible.builtin.file', role_path ~ '/files/create_managed_user.py') }}"
    tty: false
  register: paperless_managed_user_creation
  when:
    - paperless_managed_users_phase == 'reconcile'
    - not ansible_check_mode
    - paperless_managed_user_matches[item.username] | length == 0
  loop: "{{ vault_managed_paperless_ngx_users }}"
  loop_control:
    label: paperless-managed-user
  changed_when: true
  # files/create_managed_user.py commits the row and its password in one
  # transaction on purpose, so that a half-created account cannot survive; until
  # this line the rollback that protects it was invisible. The exception exited
  # nonzero, the module reported success, changed_when asserted a creation that
  # rolled back, and the re-list below then failed on a user that is absent --
  # naming the listing rather than the create that never landed.
  failed_when: paperless_managed_user_creation.rc | default(1) | int != 0
  no_log: true

- name: Re-list sanitized Paperless users after managed-user creation
  community.docker.docker_compose_v2_exec:
    project_src: "{{ platform_current_dir }}/services/paperless-ngx"
    project_name: "{{ paperless_compose_project_name }}"
    files: "{{ platform_service_compose_files['paperless-ngx'] }}"
    env_files: ["{{ platform_runtime_dir }}/services/paperless-ngx/.env"]
    service: webserver
    argv:
      - python
      - manage.py
      - shell
      - -c
      - "{{ lookup('ansible.builtin.file', role_path ~ '/files/list_users.py') }}"
    tty: false
  register: paperless_managed_users_refreshed_probe
  when:
    - paperless_managed_users_phase == 'reconcile'
    - not ansible_check_mode
  changed_when: false
  check_mode: false
  # The same read as the first listing and the same reason, one stage on: this
  # is what the created identities are re-resolved against, so a failure here
  # that reported success would send the repair below at primary keys taken from
  # a listing that was never produced.
  failed_when: paperless_managed_users_refreshed_probe.rc | default(1) | int != 0
  no_log: true

- name: Resolve refreshed sanitized Paperless users
  ansible.builtin.set_fact:
    paperless_managed_users_refreshed_listing: >-
      {{ paperless_managed_users_refreshed_probe.stdout_lines | last | from_json }}
  when:
    - paperless_managed_users_phase == 'reconcile'
    - not ansible_check_mode
  no_log: true

- name: Require exact newly created Paperless managed identities
  ansible.builtin.assert:
    that:
      - paperless_managed_users_refreshed_listing | selectattr('username', 'equalto', item.item.username) | list | length == 1
      - >-
        paperless_managed_users_refreshed_listing | map(attribute='username') |
        map('trim') | map('lower') | select('equalto', item.item.username | trim | lower) | list | length == 1
    fail_msg: Paperless did not return one exact newly created managed identity.
  loop: "{{ paperless_managed_user_creation.results | default([]) | rejectattr('skipped', 'defined') | list }}"
  loop_control:
    label: paperless-managed-user
  when:
    - paperless_managed_users_phase == 'reconcile'
    - not ansible_check_mode
  no_log: true

- name: Authenticate newly created Paperless managed users
  ansible.builtin.uri:
    url: "{{ paperless_api }}/api/token/"
    method: POST
    body_format: json
    body:
      username: "{{ item.item.username }}"
      password: "{{ item.item.password }}"
    status_code: "{{ range(100, 600) | list }}"
    return_content: true
  register: paperless_new_managed_user_authentication
  loop: "{{ paperless_managed_user_creation.results | default([]) | rejectattr('skipped', 'defined') | list }}"
  loop_control:
    label: paperless-managed-user
  when:
    - paperless_managed_users_phase == 'reconcile'
    - not ansible_check_mode
  failed_when: false
  changed_when: false
  no_log: true

- name: Require newly created Paperless managed-user credentials
  ansible.builtin.assert:
    that:
      - item.status | default(0) | int == 200
      - item.json.token | default('') | length > 0
    fail_msg: >-
      Newly created Paperless managed user does not accept its vault password. Run the reviewed
      credential-migration procedure; Ansible will not reset it or continue privilege reconciliation.
  loop: "{{ paperless_new_managed_user_authentication.results | default([]) | rejectattr('skipped', 'defined') | list }}"
  loop_control:
    label: paperless-managed-user
  when:
    - paperless_managed_users_phase == 'reconcile'
    - not ansible_check_mode
  no_log: true

- name: Authenticate effective Paperless managed users after re-list
  ansible.builtin.uri:
    url: "{{ paperless_api }}/api/token/"
    method: POST
    body_format: json
    body:
      username: "{{ item.username }}"
      password: "{{ item.password }}"
    status_code: "{{ range(100, 600) | list }}"
    return_content: true
  register: paperless_effective_managed_user_authentication
  loop: "{{ vault_managed_paperless_ngx_users }}"
  loop_control:
    label: paperless-managed-user
  when:
    - paperless_managed_users_phase == 'reconcile'
    - not ansible_check_mode
  failed_when: false
  changed_when: false
  no_log: true

- name: Require effective Paperless managed-user credentials
  ansible.builtin.assert:
    that:
      - item.status | default(0) | int == 200
      - item.json.token | default('') | length > 0
    fail_msg: >-
      A re-resolved Paperless managed user does not accept its preserved vault
      password. Run the reviewed credential-migration procedure; Ansible will
      not reset it automatically or continue privilege reconciliation.
  loop: "{{ paperless_effective_managed_user_authentication.results | default([]) | rejectattr('skipped', 'defined') | list }}"
  loop_control:
    label: paperless-managed-user
  when:
    - paperless_managed_users_phase == 'reconcile'
    - not ansible_check_mode
  no_log: true

- name: Resolve effective authenticated Paperless managed-user bindings
  ansible.builtin.set_fact:
    paperless_authenticated_managed_user_ids: >-
      {{ (paperless_authenticated_managed_user_ids | default({})) |
         combine({item.item.username:
           (paperless_managed_users_refreshed_listing |
            selectattr('username', 'equalto', item.item.username) | first).id}) }}
    paperless_authenticated_managed_user_tokens: >-
      {{ (paperless_authenticated_managed_user_tokens | default({})) |
         combine({item.item.username: item.json.token}) }}
  loop: "{{ paperless_effective_managed_user_authentication.results | default([]) | rejectattr('skipped', 'defined') | list }}"
  loop_control:
    label: paperless-managed-user
  when:
    - paperless_managed_users_phase == 'reconcile'
    - not ansible_check_mode
  no_log: true

- name: Resolve effective Paperless managed identities after creation
  ansible.builtin.set_fact:
    paperless_effective_managed_user_matches: >-
      {{ (paperless_effective_managed_user_matches | default({})) |
         combine(dict([(item.username,
           (paperless_managed_users_refreshed_listing
            if paperless_managed_users_refreshed_listing is defined
            else paperless_managed_users_listing) |
           selectattr('username', 'equalto', item.username) | list)])) }}
  loop: "{{ vault_managed_paperless_ngx_users }}"
  loop_control:
    label: paperless-managed-user
  no_log: true

- name: Report planned Paperless managed-user non-secret repair
  ansible.builtin.debug:
    msg: PAPERLESS_PLAN_MANAGED_USER_REPAIR
  when:
    - paperless_managed_users_phase == 'reconcile'
    - ansible_check_mode
    - paperless_effective_managed_user_matches[item.username] | length == 1
    - >-
      paperless_effective_managed_user_matches[item.username][0].email != item.email or
      paperless_effective_managed_user_matches[item.username][0].is_active | bool != item.is_active | bool or
      paperless_effective_managed_user_matches[item.username][0].is_staff | bool != item.is_staff | bool or
      paperless_effective_managed_user_matches[item.username][0].is_superuser | bool != item.is_superuser | bool or
      paperless_effective_managed_user_matches[item.username][0].groups | sort != item.groups | sort
  loop: "{{ vault_managed_paperless_ngx_users }}"
  loop_control:
    label: paperless-managed-user
  changed_when: true

- name: Require stable authenticated Paperless managed identities
  ansible.builtin.assert:
    that:
      - paperless_effective_managed_user_matches[item.username] | length == 1
      - >-
        paperless_effective_managed_user_matches[item.username][0].id | int ==
        paperless_authenticated_managed_user_ids[item.username] | int
      - >-
        paperless_effective_managed_user_matches[item.username][0].id | int ==
        paperless_initial_authenticated_managed_user_ids.get(
          item.username,
          paperless_effective_managed_user_matches[item.username][0].id
        ) | int
      - paperless_authenticated_managed_user_tokens[item.username] | length > 0
    fail_msg: >-
      A Paperless managed username resolved to a different primary key after
      credential proof. Run the reviewed credential-migration procedure;
      Ansible will not mutate a replacement identity.
  when:
    - not ansible_check_mode
  loop: "{{ vault_managed_paperless_ngx_users }}"
  loop_control:
    label: paperless-managed-user
  no_log: true

- name: Bind authenticated Paperless managed users to listed primary keys
  community.docker.docker_compose_v2_exec:
    project_src: "{{ platform_current_dir }}/services/paperless-ngx"
    project_name: "{{ paperless_compose_project_name }}"
    files: "{{ platform_service_compose_files['paperless-ngx'] }}"
    env_files: ["{{ platform_runtime_dir }}/services/paperless-ngx/.env"]
    service: webserver
    env:
      MANAGED_USERNAME: "{{ item.username }}"
      MANAGED_ID: "{{ paperless_authenticated_managed_user_ids[item.username] | string }}"
      MANAGED_TOKEN: "{{ paperless_authenticated_managed_user_tokens[item.username] }}"
    argv:
      - python
      - manage.py
      - shell
      - -c
      - "{{ lookup('ansible.builtin.file', role_path ~ '/files/bind_managed_identity.py') }}"
    tty: false
  register: paperless_managed_user_binding
  when: not ansible_check_mode
  loop: "{{ vault_managed_paperless_ngx_users }}"
  loop_control:
    label: paperless-managed-user
  changed_when: false
  # The most consequential of the five, because this task exists only to refuse.
  # files/bind_managed_identity.py locks the token and the row and asserts that
  # the authenticated token belongs to the primary key the repair is about to
  # write, so that a concurrent rename cannot land the repair on a different
  # account. Until this line the refusal was inert: the AssertionError exited
  # nonzero, the module reported success, and the repair below ran against the
  # identity the binding had just rejected.
  failed_when: paperless_managed_user_binding.rc | default(1) | int != 0
  no_log: true

- name: Repair Paperless managed-user non-secret properties
  community.docker.docker_compose_v2_exec:
    project_src: "{{ platform_current_dir }}/services/paperless-ngx"
    project_name: "{{ paperless_compose_project_name }}"
    files: "{{ platform_service_compose_files['paperless-ngx'] }}"
    env_files: ["{{ platform_runtime_dir }}/services/paperless-ngx/.env"]
    service: webserver
    env:
      MANAGED_USERNAME: "{{ item.username }}"
      MANAGED_ID: "{{ paperless_authenticated_managed_user_ids[item.username] | string }}"
      MANAGED_TOKEN: "{{ paperless_authenticated_managed_user_tokens[item.username] }}"
      MANAGED_EMAIL: "{{ item.email }}"
      MANAGED_ACTIVE: "{{ item.is_active | ternary('true', 'false') }}"
      MANAGED_STAFF: "{{ item.is_staff | ternary('true', 'false') }}"
      MANAGED_SUPERUSER: "{{ item.is_superuser | ternary('true', 'false') }}"
      MANAGED_GROUPS: "{{ item.groups | to_json }}"
    argv:
      - python
      - manage.py
      - shell
      - -c
      - "{{ lookup('ansible.builtin.file', role_path ~ '/files/repair_managed_user.py') }}"
    tty: false
  when:
    - paperless_managed_users_phase == 'reconcile'
    - not ansible_check_mode
    - paperless_effective_managed_user_matches[item.username] | length == 1
    - >-
      paperless_effective_managed_user_matches[item.username][0].email != item.email or
      paperless_effective_managed_user_matches[item.username][0].is_active | bool != item.is_active | bool or
      paperless_effective_managed_user_matches[item.username][0].is_staff | bool != item.is_staff | bool or
      paperless_effective_managed_user_matches[item.username][0].is_superuser | bool != item.is_superuser | bool or
      paperless_effective_managed_user_matches[item.username][0].groups | sort != item.groups | sort
  register: paperless_managed_user_repair
  loop: "{{ vault_managed_paperless_ngx_users }}"
  loop_control:
    label: paperless-managed-user
  changed_when: true
  # files/repair_managed_user.py re-proves the identity binding inside the
  # transaction that writes, and asserts that every declared group resolved --
  # "so a missing group fails the play instead of silently narrowing the user's
  # access", which is what its own comment claims and what this line is required
  # for. Both raises exited nonzero into a module that reported success, and
  # changed_when then announced a repair that had rolled back. The verify phase
  # at the bottom of this file is not the backstop it looks like: it is reached
  # through a second include and a fatal failure here stops the role before it,
  # which is the point -- the write is caught where it happens.
  failed_when: paperless_managed_user_repair.rc | default(1) | int != 0
  no_log: true

- name: Verify exact Paperless managed users
  ansible.builtin.assert:
    that:
      - paperless_managed_user_matches[item.username] | length == 1
      - paperless_managed_user_matches[item.username][0].username == item.username
      - paperless_managed_user_matches[item.username][0].email == item.email
      - paperless_managed_user_matches[item.username][0].is_active | bool == item.is_active | bool
      - paperless_managed_user_matches[item.username][0].is_staff | bool == item.is_staff | bool
      - paperless_managed_user_matches[item.username][0].is_superuser | bool == item.is_superuser | bool
      - paperless_managed_user_matches[item.username][0].groups | sort == item.groups | sort
      - >-
        paperless_managed_user_matches[item.username][0].id | int ==
        paperless_authenticated_managed_user_ids[item.username] | int
    fail_msg: A managed Paperless identity is absent, duplicated, or differs from its exact projection.
  when: paperless_managed_users_phase == 'verify'
  loop: "{{ vault_managed_paperless_ngx_users }}"
  loop_control:
    label: paperless-managed-user
  no_log: true
